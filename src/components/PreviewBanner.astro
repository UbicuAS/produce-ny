---
// Tydelig merking av at dette er et utkast på Ubicus domene, ikke kundens
// egen side i drift. Skal stå til noe annet er bestemt.
---

<div class="preview" role="note">
  <p>
    <strong>Forhåndsvisning</strong>
    <span class="preview__sep" aria-hidden="true">·</span>
    Utkast til ny nettside for Produce AS, laget av Ubicu AS. Ikke publisert –
    siden er skjult for søkemotorer.
  </p>
</div>

<style>
  .preview {
    background: repeating-linear-gradient(
      135deg,
      #2a0b0d,
      #2a0b0d 14px,
      #340e11 14px,
      #340e11 28px
    );
    border-bottom: 1px solid #58181d;
    color: #ffd7d9;
    font-size: 0.85rem;
    line-height: 1.45;
    text-align: center;
    padding: 0.6rem var(--gutter);
  }

  .preview strong {
    color: var(--accent-text);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-size: 0.78rem;
  }

  .preview__sep {
    margin-inline: 0.4rem;
    opacity: 0.5;
  }
</style>
